{
  "name": "modern-selfdrive-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Modern Selfdrive Car — public website and owner portal",
  "scripts": {
    "dev": "concurrently -n backend,public,portal -c yellow,blue,magenta \"npm run dev:backend\" \"npm run dev:public\" \"npm run dev:portal\"",
    "dev:backend": "cd server && npm run dev",
    "dev:public": "cd apps/public && npm run dev",
    "dev:portal": "cd apps/portal && npm run dev",
    "build:public": "cd apps/public && npm run build",
    "build:portal": "cd apps/portal && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../apps/public && npm install && cd ../portal && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.3"
  }
}